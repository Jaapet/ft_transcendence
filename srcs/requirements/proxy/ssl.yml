tls:
  options:
    default:
      minVersion: "VersionTLS12"                          # Minimum TLS Version
  certificates:
    - certFile: /ssl/transcendence.gmcg.fr.crt
      keyFile: /ssl/transcendence.gmcg.fr.key
    - certFile: /ssl/localhost.crt
      keyFile: /ssl/localhost.key

# http:
#   routers:
#     websocket:
#       rule: Host(`transcendence.gmcg.fr`)
#       tls: true
#       entrypoints: websocket
#   services:
#     websocket:
#       loadbalancer:
#         servers:
#           - url: "http://frontend:3001"
http:
  serversTransports:
    transport_prometheus:
      serverName: "prometheus"
      rootCAs:
        - /ssl/CA.crt
